# 🚗 CarService Backend Project

Този проект представлява RESTful API за управление на автомобилни услуги, изграден по изискванията за курс по Бекенд програмиране.

## 🏗️ Архитектура (N-tier)
Проектът е разделен на логически слоеве за по-добра поддръжка и тестване:
* **CarService.Host**: Стартов проект (API слой), съдържащ контролерите и конфигурацията.
* **CarService.Services**: Слой за бизнес логика, където са приложени правилата на приложението.
* **CarService.Data**: Слой за достъп до данни, работещ с MongoDB.

## 🛠️ Технологичен стек
* **Език**: C# / .NET 8+
* **База данни**: MongoDB (използвана чрез `IOptionsMonitor` за динамична конфигурация).
* **Logging**: Serilog за проследяване на събития.
* **Mapping**: Mapster за трансформация на обекти (Request -> Entity -> Response).
* **Validation**: FluentValidation за валидиране на входящите данни.
* **Testing**: xUnit и Moq за Unit тестове на бизнес логиката.

## ✅ Покрити изисквания
1.  **Dependency Injection**: В `CarBusinessService` са инжектирани два интерфейса (`ICarRepository` и `INotificationService`).
2.  **Health Checks**: Реализиран ендпойнт `/health` за проверка на състоянието на системата.
3.  **Unit Tests**: Написани са позитивни и негативни тестове с Mock обекти.
4.  **Swagger**: Автоматична документация на API-то.
5.  **DTS/Requests/Responses**: Всеки ендпойнт използва специфични обекти за трансфер на данни, без да излага директно моделите от базата.

## 🧪 Стартиране на тестовете
```bash
dotnet test
